{
    "build": {
      "env": {
        "DATABASE_URL": "@database_url"
      }
    },
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next",
        "config": {
          "includeFiles": ["prisma/**"]
        }
      }
    ]
  }